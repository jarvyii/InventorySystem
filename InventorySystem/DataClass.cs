using System.Data.SQLite;
namespace InventorySystem
{
    class DataClass
    {
        // public SQLiteConnection dbConnection;

      /*  public static SQLiteConnection OpenDB()
        {
            SQLiteConnection dbconnection = new SQLiteConnection("Data Source=C:/JARV/WEBPAGE/Code Louisville/C#/project/InventorySystem/InventorySystem/DB/inventorydb.db;Version=3;");
                //System.Console.WriteLine(dbConnection);
            dbconnection.Open();
            return dbconnection;*/
                /*
                string sql = "insert into products (description, barcode) values ('Pencil 1', '4000')";
                SQLiteCommand command = new SQLiteCommand(sql, dbConnection);
                command.ExecuteNonQuery();
                closeDB(); 
        }

        public void closeDB(SQLiteConnection dbConnection ) => dbConnection.Close(); */
    }
}
